# Forecast models

Organize baseline, statistical, machine-learning, and ensemble implementations separately. Register every trained version with its data window, feature set, time-ordered validation method, metrics, and artifact location.

The initial project skeleton contains no trained model and emits no forecast. Add a model only after it can be compared with a simple baseline on a time-ordered validation and independent test interval.
